<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="org.noodle.RecipeImageMapper">

	<insert id="create">
		insert 
			into tbl_recipeImage (bno, ino, step, image, thumbnail)
		values 
			(#{bno}, seq_recipeImage.nextval, #{step}, #{image}, #{thumbnail})
	</insert>
	
	<select id="read" resultType="RecipeImageVO">
		select * from tbl_recipeImage where ino =#{ino}
	</select>

	<delete id="delete">
		delete tbl_recipeImage where ino = #{ino}
	</delete>	
	
	<select id="readStep" resultType="RecipeImageVO">
		select * from tbl_recipeImage where bno = #{bno} and step = #{step}
	</select>
	
</mapper>